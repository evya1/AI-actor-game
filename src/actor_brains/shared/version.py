"""Single source of truth for the actor_brains package version.

Kept in sync with ``pyproject.toml`` and ``config/actor_config.json`` per the
project versioning rule (CLAUDE.md §6).
"""

from __future__ import annotations

__version__ = "1.01"
